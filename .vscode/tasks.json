{
    version: "2.0.0",
    tasks: [
        {
            label: "Run Tests Quick",
            name: "run-tests",
            type: "shell",
            command: "ctest --test-dir build --config Debug --output-on-failure -j 4",
            group: {
                kind: "test",
                isDefault: true
            },
            problemMatcher: ["$msCompile"]
        }
    ]
}