{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Tests Quick",
            "type": "shell",
            "command": "ctest --test-dir build -C Debug --output-on-failure -j 4",
            "group": {
                "kind": "test",
                "isDefault": true
            },
            "problemMatcher": ["$msCompile"]
        }
    ]
}